# MongoDB connection (replace with your own credentials)
DATABASE=mongodb+srv://<USERNAME>:<PASSWORD>@cluster0.xjgfccx.mongodb.net/brocowallet?retryWrites=true&w=majority&appName=Cluster0

# Password for MongoDB user (keep it secret!)
DATABASE_PASSWORD=<YOUR_STRONG_PASSWORD>

# JWT secret key for token signing (generate a strong random one!)
JWT_SECRET=<YOUR_VERY_LONG_RANDOM_SECRET_HERE>

# JWT token expiration
JWT_EXPIRES_IN=90d
JWT_COOKIE_EXPIRES_IN=90